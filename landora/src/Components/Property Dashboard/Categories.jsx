import React from "react";
import { Container, Typography } from "@mui/material";

const Categories = () => {
  return (
    <Container>
      <Typography variant="h4">Manage Categories & Types</Typography>
      {/* Add category management UI */}
    </Container>
  );
};

export default Categories;
